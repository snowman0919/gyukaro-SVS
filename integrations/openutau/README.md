# OpenUtau bridge stub

Export note/lyric timing as renderer-protocol JSON, then invoke `gyu-singer render`. Native OpenUtau renderer integration is not shipped because v1 lacks phoneme-conditioned synthesis.
