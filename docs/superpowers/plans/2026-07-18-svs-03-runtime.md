# SVS-03 Runtime Safety Implementation Plan

1. Freeze all implemented backend names and statuses in a machine-readable registry.
2. Test fail-closed default rendering, RC8/RC9 denial, explicit override logging, release-path denial, and registry drift.
3. Route CLI render and serve through the policy while leaving the frontend command independent.
4. Freeze the least-invasive identity candidate order, references, seeds, preservation metrics, and valid outcomes.
5. Add zero-equivalent FiLM and low-rank experiment adapters without importing them from production runtime.
6. Enforce Korean foundation authorization before optimizer initialization.
7. Generate a compact reproducible runtime-safety report.
8. Run full tests, dataset validation, evidence validation, and diff checks; commit without WAVs or checkpoints.
